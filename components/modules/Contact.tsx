import { sections } from "@/constants/sections";

export default function Contact() {
    return (
        <section id={sections[2].id} className="bg-red-400">
            CONTATO
        </section>
    )
}
